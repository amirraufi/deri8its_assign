# requirements.txt
numpy>=1.26          # arrays, math, polyfit input for smile
pandas>=2.1          # DataFrames, groupby/join, CSV IO
scipy>=1.10          # brentq solver, stats.norm, interpolate.interp1d
matplotlib>=3.8      # plotting IV smiles & time‑series charts
websockets>=12.0     # async WebSocket used by DeribitStream
